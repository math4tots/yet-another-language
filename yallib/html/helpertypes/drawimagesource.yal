


export interface DrawImageSource {
  """
  Helper type for marking types that can be used as images in
  in CanvasRenderingContext2D.drawImage()

  The purpose of using a marked interface like this instead of
  a union is to avoid creating import cycles
  """
  const __marker_DrawImageSource: Never
}
