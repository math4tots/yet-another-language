from './mixin/drawimagesource' import DrawImageSource


export interface ImageBitmap extends DrawImageSource {
  const __marker_ImageBitmap: Any

  const height: Number = aliasFor(__js_height)
  const width: Number = aliasFor(__js_width)
}

export const createImageBitmap =
  native createImageBitmap(image: DrawImageSource): ImageBitmap "createImageBitmap(image)"
