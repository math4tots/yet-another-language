

const PI = 3.14159265358979323846
const TAU "Two times PI" = 2 * PI

const sin "computes sine in radians" = native(x: Number): Number {
  js "return NumberValue.of(Math.sin(x.value))"
}

const cos "computes cosine in radians" = native(x: Number): Number {
  js "return NumberValue.of(Math.cos(x.value))"
}

const tan "computes tangent in radians" = native(x: Number): Number {
  js "return NumberValue.of(Math.tan(x.value))"
}
