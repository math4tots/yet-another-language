
export interface Math {
  static {
    aliasFor(native constexpr "Math")

    const PI: Number = aliasFor(__js_PI)

    function sin(x: Number): Number {
      "compute sine in radians"
      aliasFor(__js_sin)
    }
    function cos(x: Number): Number {
      "compute cosine in radians"
      aliasFor(__js_cos)
    }
    function tan(x: Number): Number {
      "compute tangent in radians"
      aliasFor(__js_tan)
    }
  }
}
