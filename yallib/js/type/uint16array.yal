from './arraybuffer' import ArrayBuffer
from './arraybufferview' import ArrayBufferView
from './typedarray' import TypedArray


export interface Uint16Array extends TypedArray {
  static {
    aliasFor(native constexpr "Uint16Array")

    function new(valuesOrLength: List[Number] | TypedArray | ArrayBuffer | Number): Uint16Array {
      aliasFor(__op_new__)
    }

    function new(buffer: ArrayBuffer, byteOffset: Number, length: Number): Uint16Array {
      aliasFor(__op_new__)
    }

    function new(buffer: ArrayBuffer, byteOffset: Number): Uint16Array {
      aliasFor(__op_new__)
    }
  }
}
